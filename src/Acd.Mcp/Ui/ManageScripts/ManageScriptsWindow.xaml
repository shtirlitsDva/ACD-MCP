<Window x:Class="Acd.Mcp.Ui.ManageScripts.ManageScriptsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Width="640" Height="420"
        Background="{DynamicResource Brush.Bg.Window}"
        Foreground="{DynamicResource Brush.Fg.Primary}"
        WindowStartupLocation="CenterOwner"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Acd.Mcp;component/Ui/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="180"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="ScriptList"
                     Grid.Column="0"
                     ItemsSource="{Binding Items}"
                     SelectedItem="{Binding Selected, Mode=TwoWay}"
                     DisplayMemberPath="Name"
                     BorderThickness="0,0,1,0"
                     BorderBrush="{DynamicResource Brush.Border.Subtle}"/>

            <Grid Grid.Column="1" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Text="{Binding Selected.Name, TargetNullValue='(no selection)'}"
                           Style="{StaticResource Text.SectionHeader}"/>
                <TextBlock Grid.Row="1"
                           Text="{Binding Selected.Summary}"
                           Style="{StaticResource Text.Secondary}"
                           TextWrapping="Wrap"
                           Margin="0,2,0,8"/>
                <Border Grid.Row="2" Style="{StaticResource Border.DetailBox}">
                    <TextBox Text="{Binding Selected.Body, Mode=OneWay}"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             FontFamily="{StaticResource Font.Mono}"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"/>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Border.SectionStrip}">
            <DockPanel LastChildFill="False">
                <Button Content="Load"
                        Command="{Binding LoadCommand}"
                        DockPanel.Dock="Left"
                        IsDefault="True"
                        Margin="0,0,6,0" MinWidth="80"/>
                <Button Content="Save as new…"
                        Command="{Binding SaveAsCommand}"
                        DockPanel.Dock="Left"
                        Margin="0,0,6,0" MinWidth="80"/>
                <Button Content="Rename…"
                        Command="{Binding RenameCommand}"
                        DockPanel.Dock="Left"
                        Margin="0,0,6,0" MinWidth="80"/>
                <Button Content="Delete"
                        Command="{Binding DeleteCommand}"
                        DockPanel.Dock="Left"
                        Margin="0,0,6,0" MinWidth="80"/>
                <Button Content="Close"
                        Command="{Binding CloseCommand}"
                        DockPanel.Dock="Right"
                        IsCancel="True"
                        MinWidth="80"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
